@layout('base')
@section('content')
<h1> Add a new category </h1>
<form method="POST">
  {{csrfField()}}
  <div>
    <label class="form-label">Category Name</label>
    <input type="text" name="name" value="{{ old('name', '') }}" class="form-control {{ hasErrorFor('name') ? 'is-invalid' : '' }}"/>
    @if(hasErrorFor('name'))
    <div class="invalid-feedback">{{ getErrorFor('name') }}</div>
    @endif
  </div>
  <input type="submit" class="btn btn-light mt-3"/>
</form>
@endsection
