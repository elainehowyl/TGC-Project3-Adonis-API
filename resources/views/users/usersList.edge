@layout('base')
@section('content')
<h1>Customer's List</h1>
<table class="table">
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Contact Number</th>
    <th>Email</th>
    <th>Password</th>
  </tr>
  @each(u in users)
  <tr>
    <td>{{u.first_name}}</td>
    <td>{{u.last_name}}</td>
    <td>{{u.contact_number}}</td>
    <td>{{u.email}}</td>
    <td>{{u.password}}</td>
  </tr>
  @endeach
</table>
@endsection
